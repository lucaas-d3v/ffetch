import sys;
import str;

fn get_ram() arr<string> { 
    const ram = sys.ram_usage() ~> if_fail("Erro de syscall de memória");
    
    var r = str_array(); 
    
    push(r, ram["total"] ~> to_str()); 
    push(r, ram["available"] ~> to_str()); 
    
    return r;
}